<div>
    {{#each playlists}}
    <a href="/playlist/{{id}}">{{playlist_name}}</a>
    <div>
        <iframe style="border-radius:12px" src="{{embed_playlist}}" width="100%" height="380" frameBorder="0"
            allowfullscreen=""
            allow="autoplay; clipboard-write; encrypted-media; fullscreen; picture-in-picture"></iframe>
    </div>
    <br>
    {{/each}}
</div>